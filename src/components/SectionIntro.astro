---
export interface Props {
  eyebrow?: string;
  title: string;
  text?: string;
  align?: "left" | "center";
  headingId?: string;
}

const { eyebrow, title, text, align = "left", headingId } = Astro.props;
const centered = align === "center";
---

<div class:list={["space-y-4", centered && "mx-auto max-w-3xl text-center"]}>
  {eyebrow && (
    <p class="text-sm font-medium uppercase tracking-[0.22em] text-primary">
      {eyebrow}
    </p>
  )}
  <h2 id={headingId} class="text-3xl leading-tight text-foreground md:text-5xl">{title}</h2>
  {text && <p class:list={["text-lg leading-8 text-muted", !centered && "max-w-3xl"]}>{text}</p>}
</div>
