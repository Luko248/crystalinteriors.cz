---
import BaseLayout from "@/layouts/BaseLayout.astro";
import SiteHeader from "@/components/SiteHeader.astro";
import SiteFooter from "@/components/SiteFooter.astro";
import SectionIntro from "@/components/SectionIntro.astro";
import { site } from "@/data/site";
---

<BaseLayout
  title="O nás"
  description="Crystal Interiors tvoří dvě zkušené designérky zaměřené na interiéry, nábytek na míru a funkční realizaci."
  pathname="/o-nas/"
  breadcrumbs={[
    { name: "Úvod", href: "/" },
    { name: "O nás", href: "/o-nas/" },
  ]}
>
  <SiteHeader pathname={Astro.url.pathname} />
  <main id="main" class="mx-auto max-w-5xl px-4 py-12 sm:px-6 sm:py-16 lg:px-8 lg:py-24">
    <SectionIntro eyebrow="O studiu" title={site.about.title} text={site.about.lead} />

    <div class="mt-10 space-y-6 rounded-3xl border border-border bg-surface p-6 sm:mt-12 sm:space-y-8 sm:p-8 md:p-12">
      {site.about.paragraphs.map((paragraph) => <p class="text-base leading-7 text-muted sm:text-lg sm:leading-9">{paragraph}</p>)}
    </div>
  </main>
  <SiteFooter />
</BaseLayout>
